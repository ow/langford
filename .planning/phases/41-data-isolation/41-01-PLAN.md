---
phase: 41-data-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/41-01-add-municipality-id-to-rpcs.sql
  - apps/web/app/services/meetings.ts
  - apps/web/app/services/matters.ts
  - apps/web/app/services/bylaws.ts
  - apps/web/app/services/organizations.ts
  - apps/web/app/services/elections.ts
  - apps/web/app/services/site.ts
  - apps/web/app/services/admin.ts
  - apps/web/app/services/subscriptions.ts
  - apps/web/app/routes/meetings.tsx
  - apps/web/app/routes/meeting-explorer.tsx
  - apps/web/app/routes/meeting-documents.tsx
  - apps/web/app/routes/speaker-alias.tsx
  - apps/web/app/routes/matters.tsx
  - apps/web/app/routes/matter-detail.tsx
  - apps/web/app/routes/bylaws.tsx
  - apps/web/app/routes/bylaw-detail.tsx
  - apps/web/app/routes/elections.tsx
  - apps/web/app/routes/election-detail.tsx
  - apps/web/app/routes/home.tsx
  - apps/web/app/routes/about.tsx
  - apps/web/app/routes/admin-people.tsx
  - apps/web/app/routes/api.digest.tsx
  - apps/web/app/routes/api.subscribe.tsx
autonomous: true
requirements: [SCOPE-01, SCOPE-02]

must_haves:
  truths:
    - "Service functions for meetings, matters, bylaws, orgs, elections, site, admin, and subscriptions all require and use municipality_id"
    - "All 13 Supabase RPCs accept filter_municipality_id parameter with DEFAULT NULL for backward compat"
    - "Route loaders pass municipality to service functions"
  artifacts:
    - path: "supabase/migrations/41-01-add-municipality-id-to-rpcs.sql"
      provides: "Municipality filtering for all hybrid_search_*, match_*, get_meetings_with_stats, and speaking_time RPCs"
      contains: "filter_municipality_id"
    - path: "apps/web/app/services/meetings.ts"
      provides: "Municipality-scoped meeting queries"
      contains: "municipality_id"
    - path: "apps/web/app/services/matters.ts"
      provides: "Municipality-scoped matter queries"
      contains: "municipality_id"
  key_links:
    - from: "apps/web/app/routes/meetings.tsx"
      to: "apps/web/app/services/meetings.ts"
      via: "municipality object passed as argument"
      pattern: "getMeetings.*municipality"
    - from: "apps/web/app/services/hybrid-search.server.ts"
      to: "supabase/migrations/41-01-add-municipality-id-to-rpcs.sql"
      via: "filter_municipality_id RPC parameter"
      pattern: "filter_municipality_id"
---

<objective>
Add municipality_id filtering to all Supabase RPCs and the simpler service layer files (direct `.eq()` pattern), then update route loaders to pass municipality through.

Purpose: Foundation layer -- RPCs must exist before search/RAG plans can pass municipality_id, and direct-table services demonstrate the pattern.
Output: SQL migration file, 8 updated service files, ~14 updated route loaders.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-data-isolation/41-CONTEXT.md
@.planning/phases/41-data-isolation/41-RESEARCH.md

@apps/web/app/services/meetings.ts
@apps/web/app/services/matters.ts
@apps/web/app/services/bylaws.ts
@apps/web/app/services/organizations.ts
@apps/web/app/services/elections.ts
@apps/web/app/services/site.ts
@apps/web/app/services/admin.ts
@apps/web/app/services/subscriptions.ts
@apps/web/app/services/municipality.ts
@apps/web/app/lib/municipality-helpers.ts
@apps/web/app/lib/types.ts
@apps/web/app/root.tsx

<interfaces>
<!-- Municipality type from root loader, available to all routes -->
From apps/web/app/lib/types.ts:
- Municipality interface with id, slug, name, short_name, meta, rss_url fields

From apps/web/app/lib/municipality-helpers.ts:
- getMunicipalityFromMatches(matches) -> Municipality | undefined

From apps/web/app/root.tsx loader:
- Returns { municipality, user, userProfile } -- municipality is available in all child routes

From apps/web/app/services/municipality.ts:
- getMunicipality(supabase, slug?) -> Municipality
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration -- add filter_municipality_id to all RPCs</name>
  <files>supabase/migrations/41-01-add-municipality-id-to-rpcs.sql</files>
  <action>
Create a single SQL migration file that adds `filter_municipality_id bigint DEFAULT NULL` parameter to ALL of these RPC functions using CREATE OR REPLACE:

**Hybrid search RPCs (from supabase/migrations/create_hybrid_search_rpcs.sql, 31-01, 21-01, 21-02):**
1. `hybrid_search_motions` -- add JOIN on meetings, filter `mt.municipality_id = filter_municipality_id`
2. `hybrid_search_key_statements` -- filter directly on `ks.municipality_id = filter_municipality_id` (has direct column)
3. `hybrid_search_document_sections` -- filter directly on `ds.municipality_id = filter_municipality_id` (has direct column)
4. `hybrid_search_bylaw_chunks` -- JOIN bylaws, filter `b.municipality_id = filter_municipality_id`

**Match RPCs (from sql/bootstrap.sql):**
5. `match_motions` -- JOIN meetings, filter `mt.municipality_id = filter_municipality_id`
6. `match_matters` -- filter directly `m.municipality_id = filter_municipality_id`
7. `match_agenda_items` -- JOIN meetings, filter `mt.municipality_id = filter_municipality_id`
8. `match_key_statements` -- filter directly `ks.municipality_id = filter_municipality_id`
9. `match_bylaws` -- filter directly `b.municipality_id = filter_municipality_id` (bylaws has municipality_id)

**Stats/profiling RPCs (from supabase/migrations/):**
10. `get_meetings_with_stats` -- filter directly `m.municipality_id = filter_municipality_id`
11. `get_speaking_time_stats` -- JOIN meetings, filter by municipality_id
12. `get_speaking_time_by_meeting` -- JOIN meetings, filter by municipality_id
13. `get_speaking_time_by_topic` -- JOIN meetings, filter by municipality_id

For EVERY function:
- Read the existing migration file that defines it to get the full current signature and body
- Add `filter_municipality_id bigint DEFAULT NULL` as the LAST parameter (preserves backward compat)
- Add `AND (filter_municipality_id IS NULL OR {table}.municipality_id = filter_municipality_id)` to WHERE clauses
- For tables without direct municipality_id (motions, agenda_items, transcript_segments), JOIN meetings and filter on `meetings.municipality_id`
- Use CREATE OR REPLACE to avoid DROP/recreate issues

IMPORTANT: Read each source migration file fully before writing the replacement. The function bodies are complex with CTEs -- you must preserve the full logic and only add the municipality filter.
  </action>
  <verify>
    <automated>cd /Users/kyle/development/viewroyal && grep -c "filter_municipality_id" supabase/migrations/41-01-add-municipality-id-to-rpcs.sql</automated>
  </verify>
  <done>All 13 RPCs have filter_municipality_id parameter with DEFAULT NULL, applied via CREATE OR REPLACE. Existing callers without the parameter continue to work (NULL = no filter).</done>
</task>

<task type="auto">
  <name>Task 2: Scope direct-table service files + update route loaders</name>
  <files>
    apps/web/app/services/meetings.ts
    apps/web/app/services/matters.ts
    apps/web/app/services/bylaws.ts
    apps/web/app/services/organizations.ts
    apps/web/app/services/elections.ts
    apps/web/app/services/site.ts
    apps/web/app/services/admin.ts
    apps/web/app/services/subscriptions.ts
    apps/web/app/routes/meetings.tsx
    apps/web/app/routes/meeting-explorer.tsx
    apps/web/app/routes/meeting-documents.tsx
    apps/web/app/routes/speaker-alias.tsx
    apps/web/app/routes/matters.tsx
    apps/web/app/routes/matter-detail.tsx
    apps/web/app/routes/bylaws.tsx
    apps/web/app/routes/bylaw-detail.tsx
    apps/web/app/routes/elections.tsx
    apps/web/app/routes/election-detail.tsx
    apps/web/app/routes/home.tsx
    apps/web/app/routes/about.tsx
    apps/web/app/routes/admin-people.tsx
    apps/web/app/routes/api.digest.tsx
    apps/web/app/routes/api.subscribe.tsx
  </files>
  <action>
**Per user decision:** Service functions receive the full municipality object (not just ID). Import Municipality type from `~/lib/types`.

**Service file changes -- add `municipality: Municipality` parameter to every exported function and add `.eq("municipality_id", municipality.id)` to queries:**

1. **meetings.ts** -- `getMeetings`, `getMeetingById`, `getDividedDecisions`, `getDocumentSections`, `getExtractedDocuments`, and any other exported functions. For direct meetings queries use `.eq("municipality_id", municipality.id)`. For motions/votes sub-queries inside these functions, use `!inner` join on meetings: `.select("..., meetings!inner(municipality_id)").eq("meetings.municipality_id", municipality.id)`.

2. **matters.ts** -- `getMatters`, `getMatterById`, `getDocumentsForAgendaItems`, `getHotTopics`, `getFiscalData`. Direct `.eq("municipality_id", municipality.id)` on matters table. For agenda_items sub-queries, use `!inner` join on meetings.

3. **bylaws.ts** -- `getBylaws`, `getBylawById`. Direct `.eq("municipality_id", municipality.id)`.

4. **organizations.ts** -- `getOrganizations`, `getOrganizationById`. Direct `.eq("municipality_id", municipality.id)`. CRITICAL: This prevents the `.single()` collision when both View Royal and Esquimalt have "Council" orgs.

5. **elections.ts** -- `getElections`, `getElectionById`. Direct `.eq("municipality_id", municipality.id)`.

6. **site.ts** -- `getAboutStats`, `getHomeData`, `getPublicNotices`. Add municipality parameter. For getAboutStats and getHomeData, add `.eq("municipality_id", municipality.id)` to meetings/matters/motions queries. getPublicNotices takes an RSS URL (already municipality-specific from municipality.rss_url), so it may just need the signature change for consistency.

7. **admin.ts** -- `getAllPeople`, `getPerson`, `updatePerson`, `createPerson`, `getAllOrganizations`, `addMembership`. Scope via `.eq("municipality_id", municipality.id)` on organizations queries. People queries scope via organization membership.

8. **subscriptions.ts** -- `getSubscriptions`, `addSubscription`, `checkSubscription`, `getMeetingDigest`, `findMattersNear`. User-scoped by user_id but matter/person lookups need municipality scoping. Add municipality parameter and scope matter/meeting queries.

**Route loader changes -- pass municipality to service calls:**

For each route that calls the above services, the loader must obtain the municipality object. Two patterns exist:

Pattern A (routes that already load municipality): home.tsx, meeting-detail.tsx -- already call `getMunicipality()`. Pass the result to service functions.

Pattern B (routes using admin client without municipality): meetings.tsx, matters.tsx, bylaws.tsx, etc. -- Add `getMunicipality` import from `~/services/municipality` and call it at the top of the loader, then pass to service functions. Use `getSupabaseAdminClient()` for the municipality lookup (same client already in use).

Specific route updates:
- **meetings.tsx**: `getMunicipality(supabase)` then `getMeetings(supabase, municipality, options)`
- **meeting-explorer.tsx**: `getMunicipality(supabase)` then `getMeetingById(supabase, municipality, slug)`
- **meeting-documents.tsx**: `getMunicipality(supabase)` then `getExtractedDocumentsForMeeting(supabase, municipality, meetingId)`
- **speaker-alias.tsx**: `getMunicipality(supabase)` then `getMeetingById(supabase, municipality, meetingId)`
- **matters.tsx**: `getMunicipality(supabase)` then `getMatters(supabase, municipality, options)`
- **matter-detail.tsx**: `getMunicipality(supabase)` then `getMatterById(supabase, municipality, id)`
- **bylaws.tsx**: `getMunicipality(supabase)` then `getBylaws(supabase, municipality)`
- **bylaw-detail.tsx**: `getMunicipality(supabase)` then `getBylawById(supabase, municipality, id)`
- **elections.tsx**: `getMunicipality(supabase)` then `getElections(supabase, municipality)`
- **election-detail.tsx**: `getMunicipality(supabase)` then `getElectionById(supabase, municipality, id)`
- **home.tsx**: Already has municipality -- pass it to `getHomeData(supabase, municipality)` (replace the `municipality.meta` pattern)
- **about.tsx**: `getMunicipality(supabase)` then `getAboutStats(supabase, municipality)`
- **admin-people.tsx**: `getMunicipality(supabase)` then pass to admin service calls
- **api.digest.tsx**: `getMunicipality(supabase)` then pass to `getMeetingDigest`
- **api.subscribe.tsx**: `getMunicipality(supabase)` then pass to subscription service calls

After all changes, run `cd apps/web && pnpm typecheck` to verify no type errors.
  </action>
  <verify>
    <automated>cd /Users/kyle/development/viewroyal/apps/web && pnpm typecheck</automated>
  </verify>
  <done>All 8 service files accept municipality parameter and filter queries by municipality_id. All route loaders pass municipality to service calls. TypeScript compilation passes with no errors.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes in apps/web (no broken imports or type mismatches)
- Every exported function in the 8 service files has a `municipality` parameter
- SQL migration file contains all 13 RPC replacements with `filter_municipality_id`
- grep for unscoped queries: `grep -rn "from\(\"meetings\"\)" apps/web/app/services/meetings.ts` should show `.eq("municipality_id"` on every query chain
</verification>

<success_criteria>
- All direct-table service functions (meetings, matters, bylaws, orgs, elections, site, admin, subscriptions) accept and filter by municipality
- All 13 Supabase RPCs accept filter_municipality_id with DEFAULT NULL
- Route loaders pass municipality through to service functions
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/41-data-isolation/41-01-SUMMARY.md`
</output>
