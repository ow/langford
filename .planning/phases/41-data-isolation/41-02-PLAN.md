---
phase: 41-data-isolation
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - apps/web/app/services/people.ts
  - apps/web/app/services/search.ts
  - apps/web/app/services/hybrid-search.server.ts
  - apps/web/app/services/vectorSearch.ts
  - apps/web/app/services/profiling.ts
  - apps/web/app/services/analytics.ts
  - apps/web/app/routes/people.tsx
  - apps/web/app/routes/person-profile.tsx
  - apps/web/app/routes/person-proposals.tsx
  - apps/web/app/routes/person-votes.tsx
  - apps/web/app/routes/compare.tsx
  - apps/web/app/routes/alignment.tsx
  - apps/web/app/routes/search.tsx
  - apps/web/app/routes/api.search.tsx
autonomous: true
requirements: [SCOPE-01, SCOPE-02]

must_haves:
  truths:
    - "People queries return only councillors from the current municipality's organizations"
    - "Hybrid search passes filter_municipality_id to all RPCs"
    - "Vector search passes filter_municipality_id to all match_* RPCs"
    - "Global search aggregates only municipality-scoped results"
    - "Profiling and analytics queries scope through municipality organizations"
  artifacts:
    - path: "apps/web/app/services/hybrid-search.server.ts"
      provides: "Municipality-scoped hybrid search"
      contains: "filter_municipality_id"
    - path: "apps/web/app/services/vectorSearch.ts"
      provides: "Municipality-scoped vector search"
      contains: "filter_municipality_id"
    - path: "apps/web/app/services/people.ts"
      provides: "Municipality-scoped people queries"
      contains: "municipality"
    - path: "apps/web/app/services/search.ts"
      provides: "Municipality-scoped global search"
      contains: "municipality"
  key_links:
    - from: "apps/web/app/services/hybrid-search.server.ts"
      to: "supabase RPCs"
      via: "filter_municipality_id parameter in .rpc() calls"
      pattern: "filter_municipality_id.*municipality"
    - from: "apps/web/app/services/people.ts"
      to: "apps/web/app/services/organizations.ts"
      via: "Organization lookup scoped by municipality"
      pattern: "municipality_id"
---

<objective>
Scope the complex service files: people (org-based joins), search (aggregates multiple sources), hybrid-search (RPC callers), vectorSearch (RPC callers), profiling (RPC + join patterns), and analytics. Update their route loaders.

Purpose: These files have more complex scoping patterns (organization joins, RPC parameter passing, multi-source aggregation) and depend on the RPCs created in Plan 01.
Output: 6 updated service files, ~8 updated route loaders.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-data-isolation/41-CONTEXT.md
@.planning/phases/41-data-isolation/41-RESEARCH.md
@.planning/phases/41-data-isolation/41-01-SUMMARY.md

@apps/web/app/services/people.ts
@apps/web/app/services/search.ts
@apps/web/app/services/hybrid-search.server.ts
@apps/web/app/services/vectorSearch.ts
@apps/web/app/services/profiling.ts
@apps/web/app/services/analytics.ts
@apps/web/app/lib/types.ts

<interfaces>
<!-- From Plan 01: municipality parameter pattern established -->
All service functions now accept Municipality object as parameter.
Route loaders obtain municipality via getMunicipality(supabase) and pass to services.

<!-- From Plan 01: RPCs now accept filter_municipality_id -->
All hybrid_search_* and match_* RPCs accept filter_municipality_id bigint DEFAULT NULL.
Pass municipality.id to these parameters.

<!-- Pattern for organization-scoped people queries -->
From apps/web/app/api/endpoints/ (reference pattern):
- Find council org: .eq("classification", "Council").eq("municipality_id", muni.id).single()
- Then use org.id to scope people via memberships
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope search stack -- hybrid-search, vectorSearch, search</name>
  <files>
    apps/web/app/services/hybrid-search.server.ts
    apps/web/app/services/vectorSearch.ts
    apps/web/app/services/search.ts
    apps/web/app/routes/search.tsx
    apps/web/app/routes/api.search.tsx
  </files>
  <action>
**Per user decision:** Add municipality_id parameter to Postgres search RPCs at query level, not post-query. Service functions receive full municipality object.

**hybrid-search.server.ts (426 lines):**
- Add `municipality: Municipality` parameter to `hybridSearchMotions`, `hybridSearchKeyStatements`, `hybridSearchDocumentSections`, `ftsSearchTranscriptSegments`, `hybridSearchAll`, and any other exported functions
- For every `.rpc("hybrid_search_*", {...})` call, add `filter_municipality_id: municipality.id` to the params object
- For FTS queries that use `.from()` instead of RPCs, add `.eq("municipality_id", municipality.id)` or `!inner` join pattern as appropriate
- `hybridSearchAll` calls the individual search functions -- pass municipality through to each

**vectorSearch.ts (212 lines):**
- Add `municipality: Municipality` parameter to `searchMotions`, `searchMatters`, `searchAgendaItems`, `searchKeyStatements`, `vectorSearchAll`, and any other exported functions
- For every `.rpc("match_*", {...})` call, add `filter_municipality_id: municipality.id` to the params object

**search.ts (629 lines):**
- Add `municipality: Municipality` parameter to `keywordSearch`, `vectorSearch` (if it has one), `globalSearch`, `politicalAnalysis`, and any other exported functions
- Thread municipality through to all sub-function calls (hybridSearchAll, vectorSearchAll, etc.)
- Ensure the global search aggregation passes municipality to every individual search source

**Route loaders:**
- **search.tsx**: Obtain municipality in loader, pass to any service calls
- **api.search.tsx**: Already imports getMunicipality. Pass municipality to search/RAG calls. Note: RAG scoping is handled in Plan 03, but search.ts calls should be scoped here.
  </action>
  <verify>
    <automated>cd /Users/kyle/development/viewroyal/apps/web && pnpm typecheck</automated>
  </verify>
  <done>All hybrid search, vector search, and global search functions accept municipality and pass filter_municipality_id to RPCs. Route loaders pass municipality through. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Scope people, profiling, analytics + route loaders</name>
  <files>
    apps/web/app/services/people.ts
    apps/web/app/services/profiling.ts
    apps/web/app/services/analytics.ts
    apps/web/app/routes/people.tsx
    apps/web/app/routes/person-profile.tsx
    apps/web/app/routes/person-proposals.tsx
    apps/web/app/routes/person-votes.tsx
    apps/web/app/routes/compare.tsx
    apps/web/app/routes/alignment.tsx
  </files>
  <action>
**Per user decision:** Scope people via organization membership, not by adding municipality_id to people table.

**people.ts (572 lines):**
- Add `municipality: Municipality` parameter to ALL exported functions: `getPeopleWithStats`, `getRawPeopleData`, `getPersonProfile`, `getPersonProposals`, `getActiveCouncilMembers`, `getPeopleNames`, `get_person_by_role`, `fetchRelevantVotesForAlignment`, and any others
- For council member queries: find the council org using `.eq("classification", "Council").eq("municipality_id", municipality.id)` instead of just `.eq("classification", "Council")`
- For person profile queries that fetch motions/votes: use `!inner` join on meetings to filter by municipality
- For voting record queries: scope via meeting municipality_id
- CRITICAL: Any `.single()` call on organizations MUST include `.eq("municipality_id", municipality.id)` to prevent "multiple rows returned" error when Esquimalt exists

**profiling.ts (243 lines):**
- Add `municipality: Municipality` parameter to `getSpeakingTimeStats`, `getSpeakingTimeByMeeting`, `getSpeakingTimeByTopic`, `getCouncillorStances`, `getCouncillorHighlights`, `getKeyVotes`
- For RPC calls (speaking time RPCs), add `filter_municipality_id: municipality.id` to params
- For direct queries, add municipality scoping via organization or meeting joins

**analytics.ts (59 lines):**
- Add `municipality: Municipality` parameter to `getVotingAlignment`, `fetchAllVotesForAlignment`
- Scope via organization municipality_id for council member lookups

**Route loaders -- all need municipality:**
- **people.tsx**: `getMunicipality(supabase)` then pass to `getPeopleWithStats`, `getActiveCouncilMembers`, etc.
- **person-profile.tsx**: `getMunicipality(supabase)` then pass to `getPersonProfile`, profiling calls
- **person-proposals.tsx**: `getMunicipality(supabase)` then pass to `getPersonProposals`
- **person-votes.tsx**: `getMunicipality(supabase)` then pass to `getRawPeopleData`
- **compare.tsx**: `getMunicipality(supabase)` then pass to `getPeopleWithStats`, `fetchRelevantVotesForAlignment`, profiling calls
- **alignment.tsx**: `getMunicipality(supabase)` then pass to `getVotingAlignment`

After all changes, run typecheck.
  </action>
  <verify>
    <automated>cd /Users/kyle/development/viewroyal/apps/web && pnpm typecheck</automated>
  </verify>
  <done>People, profiling, and analytics services scope through municipality organizations. All route loaders pass municipality. TypeScript compilation passes with no errors.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes in apps/web
- `grep -rn "filter_municipality_id" apps/web/app/services/hybrid-search.server.ts` shows parameter in every RPC call
- `grep -rn "filter_municipality_id" apps/web/app/services/vectorSearch.ts` shows parameter in every RPC call
- `grep -rn "municipality_id" apps/web/app/services/people.ts` shows scoping on org queries
</verification>

<success_criteria>
- All search functions (hybrid, vector, global) pass filter_municipality_id to RPCs
- People queries scope through municipality-specific organizations
- Profiling RPCs receive filter_municipality_id
- Analytics scope through municipality organizations
- All route loaders obtain and pass municipality
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/41-data-isolation/41-02-SUMMARY.md`
</output>
